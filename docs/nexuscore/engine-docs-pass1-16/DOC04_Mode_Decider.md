# DOC 04 — Step‑0 Mode Decider

Classifier inputs:
- entity_type
- domain

Example:

schedule_job → Jobs Mode
send_message → Comms Mode
issue_invoice → Finance Mode
review_policy → Admin Mode
create_campaign → Social Media Mode

Mode determines:
Sentinel authority
UI overlay
tool access
automation routing
