# DOC 17 — Route Ownership Rules

Routes must follow naming pattern:

titan.jobs.*
titan.comms.*
titan.finance.*
titan.admin.*
titan.social.*

Example:

titan.jobs.create
titan.comms.thread.open
titan.finance.invoice.send
titan.admin.policy.review
titan.social.post.publish
