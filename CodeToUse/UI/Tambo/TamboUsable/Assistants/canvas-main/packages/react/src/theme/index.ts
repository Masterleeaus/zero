/**
 * Theme System
 *
 * Dynamic theming for Canvas based on unified config.
 */

export { applyTheme, useApplyTheme } from './apply-theme.js';
export {
  themePresets,
  defaultPreset,
  oceanPreset,
  forestPreset,
  sunsetPreset,
  corporatePreset,
  getThemePreset,
  applyThemePreset,
} from './presets.js';

