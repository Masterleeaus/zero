# Provider snippet extraction

Removed source `AppServiceProvider.php`; carry only these feature snippets into the host provider if still needed:

- Cashier ignores migrations: present
- Sanctum ignores migrations: present
- HTTPS redirect handling: present
- Cashier customer model uses Company: present
- CarbonInterval formatHuman macro: present