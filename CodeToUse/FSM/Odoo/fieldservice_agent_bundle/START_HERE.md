# START HERE

Best files for agents:
1. `agent_pack/AGENT_RUNBOOK.md`
2. `agent_pack/repo_scan/zero_main_scan_summary.md`
3. `agent_pack/matrices/module_dependency_graph.md`
4. `agent_pack/matrices/integration_matrix.csv`
5. `agent_pack/module_briefs/<pass>_<module>.md`
6. `agent_pack/pass_issues/<pass>_<module>_issue.md`

This bundle is intentionally prepared so an agent can:
- see what the host app already has
- see what each Odoo module actually does
- know which zero-main files to inspect first
- run passes in dependency-safe order